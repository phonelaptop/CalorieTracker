version: 1.0
runtime: nodejs16  # or nodejs18
build:
  commands:
    build:
      - echo "Installing dependencies"
      - npm install
run:
  runtime-version: 16  # or 18
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"